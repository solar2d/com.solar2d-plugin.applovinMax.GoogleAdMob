Sources for the plugin `plugin.applovinMax.GoogleAdMob`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.GoogleAdMob" = {
            publisherId = "com.solar2d",
        },
    },
}
```
